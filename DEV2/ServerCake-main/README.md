# git clone https://github.com/hmneto/DEV2.git && cd DEV2/ServerCake && sudo docker-compose up -d --build && sudo docker run -it servercake_server bash

# php composer.phar create-project --prefer-dist cakephp/app html
