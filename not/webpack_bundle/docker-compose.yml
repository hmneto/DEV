version: "3"
services:
  webpack:
    container_name: webpack
    build: .
    command: node app
    ports:
      - 80:80
    # volumes:
    #   - ./build:/app/new
    image: webpack
    working_dir: /app