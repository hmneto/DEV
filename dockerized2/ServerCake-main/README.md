# git clone https://github.com/hmneto/ServerCake.git && cd ServerCake && sudo docker-compose up -d --build && sudo docker run -it servercake_server bash

# php composer.phar create-project --prefer-dist cakephp/app html
